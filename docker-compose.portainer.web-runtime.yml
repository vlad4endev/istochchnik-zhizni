services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.web.runtime
    container_name: istochnik-web
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-8080}:80"
    volumes:
      - "${WEB_DIST_PATH:?WEB_DIST_PATH must be set}:/usr/share/nginx/html:ro"
