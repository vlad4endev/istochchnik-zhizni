# Зависимости и артефакты (корневой образ API + сборка web-react в Dockerfile.web)
node_modules
web-react/node_modules
dist
.git
.github
.cursor
*.log
.DS_Store

# Мобильный / десктопный клиент
.dart_tool
build
android/.gradle
ios/Pods
macos/Pods
**/ephemeral

# Документация и прочее (не нужно в контексте сборки API)
*.md
!README.md

# Локальные env (секреты не в образе)
.env
.env.*
!.env.example

# Prebuilt web bundles
release

# Тесты и линт (при необходимости раскомментируйте)
# coverage
# **/*.test.ts
